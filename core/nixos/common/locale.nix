{ config, ... }:
{
  # Select internationalisation properties.
  # It determines the language for program messages, the format for 
  # dates and times, sort order, and so on. It also determines the 
  # character set, such as UTF-8.
  i18n.defaultLocale = "en_US.UTF-8";
}
